# subgraph

`schema.graphql` + mappings — the detector. Reconciles Hedera mirror-node mint history against Sepolia anchor events so any unauthorized mint is publicly visible. One shared schema across three consortia. Subgraph Studio project slug: `quota`. Not yet implemented.
